'use client'
import{useState}from'react'
export default function Home(){const[name,setName]=useState(''),[link,setLink]=useState(''),[busy,setBusy]=useState(false);async function create(){if(!name.trim())return;setBusy(true);const r=await fetch('/api/invitations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({recipientName:name.trim()})});const j=await r.json();if(j.url)setLink(j.url);setBusy(false)}return <main className="shell"><section className="card"><div className="eyebrow">Romantic invitations</div><h1 className="title">Создать приглашение</h1><p className="subtitle">Для особенного человека</p><div className="field"><label>Имя получателя</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Имя" maxLength={80}/></div><button className="btn primary" onClick={create} disabled={busy}>{busy?'Создаю…':'Создать ссылку'}</button>{link&&<div className="result"><div className="muted">Ссылка готова</div><div className="link">{link}</div><button className="btn" onClick={()=>navigator.clipboard?.writeText(link)}>Копировать</button></div>}</section></main>}
